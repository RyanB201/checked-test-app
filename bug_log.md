# Bug Log

| Bug | Feedback | Testing | File Name | Notes | Status (%) | Deployed |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **1. Missing error boundary for failed component renders** | White screen shown if any dashboard widget fails to load data. | Set network to offline while loading home page. | `App.jsx` | Add React ErrorBoundary around main routes. | 0 | No |
| **2. Auth listener does not properly clean up** | Memory leak warning in console after repeated login/logout. | Login and logout multiple times rapidly in succession. | `supabaseClient.js` / `App.jsx` | Ensure `onAuthStateChange` listener is removed on unmount. | 0 | No |
| **3. Hardcoded primary color overriding tokens** | Inactive tab bar icons look off because CSS relies on a hardcoded color instead of token. | Navigate between tabs and observe icon color transition. | `HomeDashboard.css` | Replace hardcoded colors with `--primary-600` CSS token. | 100 | No |
| **4. Profile data displays stale data after edit** | Changing metrics in DemographicsForm does not immediate reflect on ProfilePage without refresh. | Update demographic info and switch to ProfilePage. | `ProfilePage.jsx` | Implement global state or reactive refetch for user profile. | 0 | No |
| **5. Login redirect ignores deep links** | Users clicking a specific URL are always sent to HomeDashboard upon login. | Navigate to `/profile` while logged out, login, and observe destination. | `Login.jsx` | Store `previousUrl` before login redirect. | 0 | No |
| **6. Unhandled Supabase unique constraint error on signup** | Signing up with an existing email shows a generic "Error" alert. | Sign up with an email already in the DB. | `SignUp.jsx` | Map Supabase error codes to user-friendly messages. | 0 | No |
| **7. Device connection time-out has no retry** | If Bluetooth/Device pairing fails or takes longer than 30s, UI hangs. | Mock a slow connection or disable BT and try to connect. | `ConnectDevice.jsx` | Add explicit timeout and retry button on failure. | 0 | No |
| **8. Password reset link expiration not handled gracefully** | Clicking an expired reset link goes to a blank or broken ResetPassword page. | Click a Supabase reset link older than the token expiry. | `ResetPassword.jsx` | Validate token on mount and show "Link Expired". | 0 | No |
| **9. Tab navigation causes layout shift** | Moving between Home and Profile causes the header to shift slightly. | Rapidly switch between tabs. | `HomeDashboard.jsx` / `ProfilePage.jsx` | Ensure layout wrapper padding is identical across pages. | 50 | No |
| **10. Measurements don't show unit clearly** | Result numbers appear without units (e.g. "120" instead of "120 mmHg"). | Complete a measurement and view FinalResults. | `FinalResults.jsx` | Append units to displayed metric values. | 0 | No |
| **11. Loader doesn't show for slow network queries** | UI looks frozen when querying past results on a slow 3G network. | Throttle network to Slow 3G and open Profile. | `ScreenLoader.jsx` / `ProfilePage.jsx` | Add loading state boolean tied to fetching results. | 0 | No |
| **12. Inputs miss autocomplete attribute** | Browser password managers don't recognize the email/password fields well. | Open login on Safari/Chrome and try to auto-fill. | `Login.jsx` / `SignUp.jsx` | Add `autoComplete="username"` and `current-password`. | 0 | No |
| **13. Demographics form allows negative age/weight** | Users can submit physical metrics that are impossible. | Enter " -10 " in the weight/age fields. | `DemographicsForm.jsx` | Add numeric min/max limits to input validation. | 0 | No |
| **14. Questionnaire form does not save partial progress** | Accidentally hitting refresh clears all entered questionnaire answers. | Fill questionnaire halfway, refresh the page. | `Questionnaire.jsx` | SessionStorage cache or local state persistence. | 0 | No |
| **15. Avatar image uses incorrect aspect ratio** | Uploaded horizontal images get squished in the circular avatar. | Upload a non-square image as profile picture. | `ProfilePage.jsx` | Use `object-fit: cover` on the avatar image. | 0 | No |
| **16. Mobile keyboard overlaps input fields** | Typing at the bottom of the sign-up form hides the input behind the keyboard on iOS. | Open on iOS Safari, tap the lowest input field. | `SignUp.css` | Implement `scrollIntoView()` on focus or padding bottom. | 0 | No |
| **17. Device disconnected alert spam** | Momentary drop in connection creates multiple overlapping toast notifications. | Simulate unstable connection in the device manager. | `ConnectedDevice.jsx` | Debounce the disconnect events. | 0 | No |
| **18. Dashboard health index calculation crash** | If user has no previous results, index calculation throws TypeError. | Login as a brand new user with no readings. | `HomeDashboard.jsx` | Add optional chaining or default metric fallback. | 0 | No |
| **19. Missing focus states for accessibility (a11y)** | Keyboard users cannot tell which tab or button is currently highlighted. | Use 'Tab' key to navigate the Home Dashboard. | `index.css` | Define explicit `:focus-visible` styles for interactive elements. | 0 | No |
| **20. Measuring vitals animation is CPU intensive** | The pulse animation runs unoptimized and drops frames on older devices. | Open on an older device or check render performance. | `MeasuringVitals.css` | Use `transform` and `opacity` for CSS animations exclusively. | 0 | No |
